# Production Python dependencies.
#
# ANHAARAH: энэ repo дотор Python backend (server.py / FastAPI app) БАЙХГҮЙ.
# Апп нь Next.js бөгөөд API нь app/api/[[...path]]/route.js дотор байгаа.
# Энэ файл нь зөвхөн хуучин Python blueprint-ийн
#   `pip install -r requirements-prod.txt`
# алдааг арилгана; `uvicorn server:app` эхлэх үедээ ямар ч байсан унана.

# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
email-validator>=2.1,<3.0

# --- MongoDB ---
motor>=3.4,<4.0
pymongo>=4.7,<5.0

# --- Auth (JWT_SECRET / ADMIN_PASSWORD орчны хувьсагчдад зориулж) ---
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0

# --- Misc ---
python-dotenv>=1.0,<2.0
python-multipart>=0.0.9,<1.0
requests>=2.32,<3.0
